═══════════════════════════════════════════════════════════════
   SWAGGER API DOCUMENTATION - VERCEL DEPLOYMENT GUIDE
═══════════════════════════════════════════════════════════════

✅ WHAT WAS DONE:
═══════════════════════════════════════════════════════════════

1. ✅ Swagger UI configured with optimizations for Vercel
2. ✅ Added /api-docs.json endpoint for Swagger JSON
3. ✅ Updated vercel.json with explicit routes for Swagger
4. ✅ Auto-detect Vercel URL in Swagger config
5. ✅ Added test script to verify Swagger locally

═══════════════════════════════════════════════════════════════
   HOW TO ACCESS SWAGGER ON VERCEL
═══════════════════════════════════════════════════════════════

🌐 Production URL:
   https://your-backend.vercel.app/api-docs

📄 Swagger JSON:
   https://your-backend.vercel.app/api-docs.json

🏠 Root endpoint (shows all available endpoints):
   https://your-backend.vercel.app/

═══════════════════════════════════════════════════════════════
   TESTING LOCALLY FIRST
═══════════════════════════════════════════════════════════════

Before deploying, test locally:

1. Run test server:
   npm run test:swagger

2. Open in browser:
   http://localhost:5000/api-docs

3. You should see the Swagger UI interface

═══════════════════════════════════════════════════════════════
   VERCEL DEPLOYMENT STEPS
═══════════════════════════════════════════════════════════════

1. Make sure all environment variables are set in Vercel:
   - Go to: Vercel Dashboard → Your Project → Settings → Environment Variables
   
   Required variables:
   ✓ NODE_ENV=production
   ✓ MONGODB_URI=your-mongodb-atlas-uri
   ✓ JWT_SECRET=your-secret
   ✓ JWT_REFRESH_SECRET=your-refresh-secret
   ✓ FRONTEND_URL=your-frontend-url
   ✓ API_URL=https://your-backend.vercel.app (optional, auto-detected)

2. Deploy to Vercel:
   vercel --prod

3. After deployment, test these URLs:
   ✓ https://your-backend.vercel.app/
   ✓ https://your-backend.vercel.app/health
   ✓ https://your-backend.vercel.app/api-docs
   ✓ https://your-backend.vercel.app/api-docs.json

═══════════════════════════════════════════════════════════════
   TROUBLESHOOTING
═══════════════════════════════════════════════════════════════

❌ If Swagger doesn't load:

1. Check Vercel logs:
   vercel logs your-deployment-url

2. Make sure NODE_ENV is set to "production" in Vercel

3. Verify the deployment includes swagger packages:
   - swagger-jsdoc
   - swagger-ui-express

4. Try accessing /api-docs.json first - if this works but UI doesn't,
   it's a static asset issue

5. Check Vercel function size - if too large, may timeout

═══════════════════════════════════════════════════════════════
   FEATURES ADDED
═══════════════════════════════════════════════════════════════

✨ Swagger UI Features:
   - Dark theme support
   - Persistent authorization (JWT stays after page refresh)
   - Clean interface (topbar hidden)
   - All API endpoints documented with JSDoc comments

✨ Available Endpoints in Swagger:
   - Auth (register, login, logout, refresh token)
   - Projects (CRUD with RBAC)
   - Tasks (CRUD with RBAC)
   - Users (profile management)
   - Dashboard (statistics)
   - Notifications
   - Search
   - Upload (Cloudinary)

═══════════════════════════════════════════════════════════════
   USING SWAGGER ON PRODUCTION
═══════════════════════════════════════════════════════════════

1. Open: https://your-backend.vercel.app/api-docs

2. Click "Authorize" button (top right)

3. Login first using POST /api/auth/login to get token

4. Copy the access token from response

5. Paste in Authorization popup: Bearer YOUR_TOKEN

6. Now you can test all protected endpoints!

═══════════════════════════════════════════════════════════════
   SECURITY NOTE
═══════════════════════════════════════════════════════════════

⚠️  Swagger is publicly accessible in production!

If you want to disable Swagger in production, add this in app.js:

if (process.env.NODE_ENV !== 'production') {
  app.use('/api-docs', swaggerUi.serve, swaggerUi.setup(swaggerSpec));
}

Currently it's enabled for assessment purposes.

═══════════════════════════════════════════════════════════════
